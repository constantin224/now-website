---
phase: 01-animation-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - lib/hooks/use-scroll-reveal.ts
  - lib/hooks/use-media-query.ts
autonomous: false
requirements: [ANIM-03, ANIM-04, ANIM-05]

must_haves:
  truths:
    - "User mit prefers-reduced-motion sieht keine Animationen -- Seite funktioniert statisch"
    - "useScrollReveal Hook erstellt ScrollTrigger-Animationen die bei Route-Wechsel sauber aufgeraeumt werden"
    - "Desktop-only Effekte (Parallax, Grain, Cursor) koennen via gsap.matchMedia Guard geschuetzt werden"
    - "Hook verwendet gsap.from() -- Content bleibt ohne JS sichtbar (Progressive Enhancement)"
  artifacts:
    - path: "lib/hooks/use-scroll-reveal.ts"
      provides: "Wiederverwendbarer Scroll-Reveal Hook mit reduced-motion und Desktop-Guard"
      exports: ["useScrollReveal"]
      min_lines: 25
    - path: "lib/hooks/use-media-query.ts"
      provides: "Utility-Hooks fuer reduced-motion Detection und Desktop-Check"
      exports: ["usePrefersReducedMotion", "useIsDesktop"]
  key_links:
    - from: "lib/hooks/use-scroll-reveal.ts"
      to: "lib/gsap.ts"
      via: "import { gsap, ScrollTrigger, useGSAP } from '@/lib/gsap'"
      pattern: "from.*@/lib/gsap"
    - from: "lib/hooks/use-scroll-reveal.ts"
      to: "gsap.matchMedia"
      via: "prefers-reduced-motion Guard"
      pattern: "prefers-reduced-motion.*no-preference"
---

<objective>
useScrollReveal Hook und Media-Query-Utilities erstellen -- das Herzstueck der Animation-Infrastruktur fuer alle kommenden Phasen.

Purpose: Jede Komponente kann mit einem einzigen Hook-Aufruf Scroll-Reveals bekommen, die automatisch reduced-motion respektieren, auf Mobile nicht laufen und bei Route-Wechsel sauber aufraeumt werden. Ohne diesen Hook muesste jede Komponente ihre eigene GSAP-Logik implementieren.
Output: Wiederverwendbare Hooks die in Phase 2-4 direkt eingesetzt werden.
</objective>

<execution_context>
@/Users/constantinkaiser/.claude/get-shit-done/workflows/execute-plan.md
@/Users/constantinkaiser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-animation-foundation/01-RESEARCH.md

<interfaces>
<!-- Von Plan 01 erstellt -- Executor braucht diese Exports -->

From lib/gsap.ts:
```typescript
export { gsap, ScrollTrigger, useGSAP };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Media-Query Utility-Hooks erstellen</name>
  <files>lib/hooks/use-media-query.ts</files>
  <read_first>
    - .planning/phases/01-animation-foundation/01-RESEARCH.md (Pattern 3: gsap.matchMedia fuer Responsive + Accessibility)
  </read_first>
  <action>
1. Verzeichnis `lib/hooks/` anlegen (falls noch nicht vorhanden).

2. Datei `lib/hooks/use-media-query.ts` erstellen:
   ```typescript
   "use client";
   import { useState, useEffect } from "react";

   /**
    * Prueft ob der User prefers-reduced-motion aktiviert hat.
    * Gibt true zurueck wenn Animationen reduziert werden sollen.
    * SSR-safe: gibt false zurueck auf dem Server.
    */
   export function usePrefersReducedMotion(): boolean {
     const [prefersReduced, setPrefersReduced] = useState(false);

     useEffect(() => {
       const mql = window.matchMedia("(prefers-reduced-motion: reduce)");
       setPrefersReduced(mql.matches);

       const handler = (e: MediaQueryListEvent) => setPrefersReduced(e.matches);
       mql.addEventListener("change", handler);
       return () => mql.removeEventListener("change", handler);
     }, []);

     return prefersReduced;
   }

   /**
    * Prueft ob das Geraet ein Desktop ist (min-width 768px, kein Touch).
    * SSR-safe: gibt false zurueck auf dem Server.
    */
   export function useIsDesktop(): boolean {
     const [isDesktop, setIsDesktop] = useState(false);

     useEffect(() => {
       const check = () => {
         setIsDesktop(
           window.matchMedia("(min-width: 768px)").matches
           && !("ontouchstart" in window)
         );
       };
       check();

       const mql = window.matchMedia("(min-width: 768px)");
       const handler = () => check();
       mql.addEventListener("change", handler);
       return () => mql.removeEventListener("change", handler);
     }, []);

     return isDesktop;
   }
   ```

   WICHTIG:
   - Beide Hooks sind SSR-safe (initial state false)
   - Beide reagieren auf Aenderungen (z.B. User aendert OS-Setting oder Fenstergroesse)
   - `usePrefersReducedMotion` wird fuer ANIM-03 in Komponenten verwendet die nicht ueber gsap.matchMedia laufen
   - `useIsDesktop` wird fuer ANIM-05 in Komponenten verwendet die Desktop-only Effekte steuern
  </action>
  <verify>
    <automated>cd /Users/constantinkaiser/claude-projects/now-website && test -f lib/hooks/use-media-query.ts && grep -c "export function" lib/hooks/use-media-query.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/hooks/use-media-query.ts` existiert
    - Datei enthaelt `"use client"` als erste Zeile
    - Datei exportiert `usePrefersReducedMotion` (exakter Funktionsname)
    - Datei exportiert `useIsDesktop` (exakter Funktionsname)
    - `usePrefersReducedMotion` verwendet `"(prefers-reduced-motion: reduce)"` als Media Query
    - `useIsDesktop` verwendet `"(min-width: 768px)"` und `"ontouchstart" in window`
    - Beide Hooks haben `addEventListener("change", handler)` und zugehoeriges Cleanup mit `removeEventListener`
    - Beide Hooks initialisieren State mit `false` (SSR-safe)
  </acceptance_criteria>
  <done>Zwei Utility-Hooks existieren: usePrefersReducedMotion und useIsDesktop. Beide SSR-safe mit Cleanup.</done>
</task>

<task type="auto">
  <name>Task 2: useScrollReveal Hook mit gsap.matchMedia Guard erstellen</name>
  <files>lib/hooks/use-scroll-reveal.ts</files>
  <read_first>
    - lib/gsap.ts (zentrale GSAP-Exports)
    - .planning/phases/01-animation-foundation/01-RESEARCH.md (Pattern 3: useScrollReveal Hook komplett, Anti-Patterns)
  </read_first>
  <action>
1. Datei `lib/hooks/use-scroll-reveal.ts` erstellen:
   ```typescript
   "use client";
   import { useRef } from "react";
   import { gsap, ScrollTrigger, useGSAP } from "@/lib/gsap";

   interface ScrollRevealOptions {
     /** Vertikaler Offset in Pixel (Standard: 30) */
     y?: number;
     /** Animationsdauer in Sekunden (Standard: 0.8) */
     duration?: number;
     /** Verzoegerung in Sekunden (Standard: 0) */
     delay?: number;
     /** ScrollTrigger start Position (Standard: "top 85%") */
     start?: string;
   }

   /**
    * Wiederverwendbarer Scroll-Reveal Hook.
    *
    * - Verwendet gsap.from() fuer Progressive Enhancement (Content sichtbar ohne JS)
    * - Respektiert prefers-reduced-motion via gsap.matchMedia
    * - Automatisches Cleanup bei Route-Wechsel via useGSAP
    * - Gibt eine ref zurueck die auf das zu animierende Element gesetzt wird
    *
    * Verwendung:
    * ```tsx
    * const ref = useScrollReveal({ y: 40, duration: 1 });
    * return <div ref={ref}>Animated Content</div>;
    * ```
    */
   export function useScrollReveal(options: ScrollRevealOptions = {}) {
     const ref = useRef<HTMLDivElement>(null);

     useGSAP(() => {
       const el = ref.current;
       if (!el) return;

       const mm = gsap.matchMedia();
       mm.add("(prefers-reduced-motion: no-preference)", () => {
         gsap.from(el, {
           opacity: 0,
           y: options.y ?? 30,
           duration: options.duration ?? 0.8,
           delay: options.delay ?? 0,
           ease: "power2.out",
           scrollTrigger: {
             trigger: el,
             start: options.start ?? "top 85%",
             toggleActions: "play none none none",
           },
         });
       });
     }, { scope: ref });

     return ref;
   }
   ```

   WICHTIG:
   - `useGSAP()` mit `{ scope: ref }` fuer automatisches Cleanup (ANIM-04)
   - `gsap.matchMedia()` mit `(prefers-reduced-motion: no-preference)` -- Animation laeuft NUR wenn User KEINE Reduktion will (ANIM-03)
   - `gsap.from()` statt gsap.to() -- Element ist im DOM sichtbar, GSAP animiert von unsichtbar zu sichtbar. Ohne JS bleibt Content sichtbar (Progressive Enhancement)
   - `toggleActions: "play none none none"` -- Animation spielt einmal beim Reincrollen, nicht bei jedem Scrollen
   - Kein `useEffect` verwenden -- `useGSAP` handhabt Context-Erstellung und Revert bei Unmount automatisch
   - Kein CSS `opacity: 0` auf Elementen setzen -- gsap.from() setzt das temporaer

2. Build pruefen: `npm run build` muss fehlerfrei durchlaufen.
  </action>
  <verify>
    <automated>cd /Users/constantinkaiser/claude-projects/now-website && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `lib/hooks/use-scroll-reveal.ts` existiert
    - Datei enthaelt `"use client"` als erste Zeile
    - Datei importiert aus `@/lib/gsap` (nicht direkt aus `gsap`)
    - Datei exportiert `useScrollReveal` (exakter Funktionsname)
    - Hook verwendet `useGSAP()` mit `{ scope: ref }` (nicht useEffect)
    - Hook verwendet `gsap.matchMedia()` mit `"(prefers-reduced-motion: no-preference)"`
    - Hook verwendet `gsap.from()` (nicht gsap.to)
    - Hook hat `toggleActions: "play none none none"`
    - Hook gibt `ref` zurueck (Return-Type)
    - Interface `ScrollRevealOptions` mit Feldern `y`, `duration`, `delay`, `start`
    - `npm run build` laeuft fehlerfrei durch
  </acceptance_criteria>
  <done>useScrollReveal Hook existiert mit gsap.matchMedia reduced-motion Guard, useGSAP Cleanup, gsap.from Progressive Enhancement. Build erfolgreich.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Smooth Scroll und Hook-Setup manuell verifizieren</name>
  <files>none</files>
  <action>
Manuelle Verifikation der gesamten Animation-Infrastruktur aus Plan 01 und Plan 02.
Der Executor startet `npm run dev` und leitet den User durch die Pruefschritte.
  </action>
  <verify>User bestaetigt mit "approved"</verify>
  <done>User hat Smooth Scroll auf Desktop, nativen Scroll auf Mobile und fehlerfreien Build bestaetigt.</done>
  <what-built>
    Komplette Animation-Infrastruktur: GSAP + Lenis Smooth Scroll (Desktop-only), useScrollReveal Hook mit reduced-motion Respektierung und automatischem Cleanup.
  </what-built>
  <how-to-verify>
    1. `npm run dev` starten
    2. Desktop-Browser oeffnen (z.B. http://localhost:3000/de)
    3. Scrollen -- Scroll sollte butterweich/smooth sein (Lenis aktiv)
    4. Browser DevTools oeffnen -> Toggle Device Toolbar (Mobile-Modus) -> Seite neu laden -> Scrollen -- nativer Scroll, kein Smooth-Effekt
    5. OS-Setting pruefen: In macOS Systemeinstellungen -> Bedienungshilfen -> Anzeige -> "Bewegung reduzieren" aktivieren -> Seite neu laden -> Keine Animationen sichtbar (erst relevant wenn useScrollReveal irgendwo eingesetzt wird -- fuer jetzt reicht es dass der Hook existiert und der Build durchlaeuft)
  </how-to-verify>
  <resume-signal>Schreib "approved" oder beschreib Probleme</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` laeuft fehlerfrei durch
- `lib/hooks/use-scroll-reveal.ts` exportiert useScrollReveal mit gsap.matchMedia Guard
- `lib/hooks/use-media-query.ts` exportiert usePrefersReducedMotion und useIsDesktop
- useScrollReveal verwendet useGSAP (nicht useEffect) und gsap.from (nicht gsap.to)
- Keine direkte GSAP-Imports aus `gsap` -- alles via `@/lib/gsap`
</verification>

<success_criteria>
- useScrollReveal Hook existiert und ist wiederverwendbar fuer alle Sektionen
- Hook respektiert prefers-reduced-motion via gsap.matchMedia (ANIM-03)
- Hook raeumt ScrollTrigger bei Route-Wechsel sauber auf via useGSAP (ANIM-04)
- Utility-Hooks (usePrefersReducedMotion, useIsDesktop) bereit fuer Desktop-only Guards (ANIM-05)
- Smooth Scroll auf Desktop funktioniert, Mobile hat nativen Scroll (manuell verifiziert)
</success_criteria>

<output>
After completion, create `.planning/phases/01-animation-foundation/01-02-SUMMARY.md`
</output>
