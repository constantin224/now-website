---
phase: 02-scroll-reveals-typografie
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - app/[locale]/shows/page.tsx
  - app/[locale]/press/page.tsx
autonomous: false
requirements: [ANIM-01, STRUC-01]

must_haves:
  truths:
    - "Alle Content-Sektionen auf Shows und Press Page faden/sliden beim Scrollen sichtbar ein"
    - "Section-Spacing auf Shows und Press nutzt die neuen --spacing-section Tokens"
    - "Alle 4 Content-Pages (About, Music, Shows, Press) haben konsistenten visuellen Rhythmus"
  artifacts:
    - path: "app/[locale]/shows/page.tsx"
      provides: "Shows-Page mit ScrollReveal und Spacing Tokens"
      contains: "ScrollReveal"
    - path: "app/[locale]/press/page.tsx"
      provides: "Press-Page mit ScrollReveal, Typography Tokens und Spacing Tokens"
      contains: "ScrollReveal"
  key_links:
    - from: "app/[locale]/shows/page.tsx"
      to: "components/scroll-reveal.tsx"
      via: "ScrollReveal Wrapper Import"
      pattern: "ScrollReveal"
    - from: "app/[locale]/press/page.tsx"
      to: "components/scroll-reveal.tsx"
      via: "ScrollReveal Wrapper Import"
      pattern: "ScrollReveal"
---

<objective>
ScrollReveal und Typography Tokens auf die verbleibenden Pages anwenden (Shows + Press) und alle Seiten visuell verifizieren -- damit ANIM-01 (Scroll-Reveals auf ALLEN Sektionen) vollstaendig erfuellt ist.

Purpose: ANIM-01 verlangt Scroll-Reveals auf ALLEN Content-Sektionen. Plan 02 deckt About+Music ab, dieser Plan deckt Shows+Press ab und verifiziert das Gesamtbild.
Output: Ueberarbeitete Shows- und Press-Pages, visuelle Verifikation aller Seiten.
</objective>

<execution_context>
@/Users/constantinkaiser/.claude/get-shit-done/workflows/execute-plan.md
@/Users/constantinkaiser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-scroll-reveals-typografie/02-RESEARCH.md
@.planning/phases/02-scroll-reveals-typografie/02-01-SUMMARY.md
@.planning/phases/02-scroll-reveals-typografie/02-02-SUMMARY.md

<interfaces>
<!-- ScrollReveal Wrapper aus Plan 02 Task 1 -->

From components/scroll-reveal.tsx:
```typescript
interface ScrollRevealProps {
  children: React.ReactNode;
  className?: string;
  y?: number;
  duration?: number;
  delay?: number;
  start?: string;
  as?: "div" | "section" | "article" | "aside";
}
export function ScrollReveal(props: ScrollRevealProps): JSX.Element;
```

<!-- Spacing und Typography Tokens aus Plan 01 -->

From app/globals.css:
```css
--spacing-section: clamp(4rem, 3rem + 5vw, 8rem);
--spacing-section-lg: clamp(6rem, 4rem + 8vw, 12rem);
--spacing-block: clamp(3rem, 2rem + 4vw, 6rem);
--text-h2: clamp(1.75rem, 1.4rem + 1.75vw, 3rem);
--tracking-heading: -0.01em;
--leading-heading: 1.2;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shows-Page und Press-Page mit ScrollReveal und Spacing Tokens ueberarbeiten</name>
  <files>app/[locale]/shows/page.tsx, app/[locale]/press/page.tsx</files>
  <read_first>
    - app/[locale]/shows/page.tsx
    - app/[locale]/press/page.tsx
    - components/scroll-reveal.tsx
    - app/globals.css
  </read_first>
  <action>
**Shows-Page (app/[locale]/shows/page.tsx):**

1. Import: `import { ScrollReveal } from "@/components/scroll-reveal";`

2. Section-Spacing: Aeusseres section-Element aendern zu:
   `className="pt-[var(--spacing-section-lg)] pb-[var(--spacing-section)] px-6"`

3. Bandsintown Widget mit ScrollReveal wrappen:
   ```tsx
   <ScrollReveal className="min-h-[200px]">
     <BandsintownWidget />
   </ScrollReveal>
   ```

4. Booking CTA mit ScrollReveal wrappen:
   ```tsx
   <ScrollReveal className="text-center mt-[var(--spacing-block)] pt-12 border-t border-line">
     <p>...</p>
   </ScrollReveal>
   ```
   Das bestehende `mt-16` wird zu `mt-[var(--spacing-block)]`.

5. Section Label bleibt OHNE ScrollReveal (sofort sichtbar).

**Press-Page (app/[locale]/press/page.tsx):**

1. Import: `import { ScrollReveal } from "@/components/scroll-reveal";`

2. Section-Spacing: Aeusseres section-Element aendern zu:
   `className="pt-[var(--spacing-section-lg)] pb-[var(--spacing-section)] px-6 max-w-5xl mx-auto"`

3. Pressefotos-Grid mit ScrollReveal wrappen:
   ```tsx
   <ScrollReveal className="grid grid-cols-1 md:grid-cols-2 gap-6 mb-6">
     <a href="/band-photo.jpg" ...>...</a>
     <a href="/band-photo-2.jpg" ...>...</a>
   </ScrollReveal>
   ```
   HINWEIS: ScrollReveal bekommt die Grid-Klassen als className. Die bestehenden `<a>`-Elemente werden zu Grid-Children.

4. Pressetext-Block mit ScrollReveal wrappen:
   ```tsx
   <ScrollReveal className="border-l-2 border-terracotta/30 pl-6 mb-[var(--spacing-block)] mt-12">
     <p className="text-sand/70 text-lg leading-relaxed">
       {t.press.press_bio}
     </p>
   </ScrollReveal>
   ```
   Das bestehende `mb-20` wird zu `mb-[var(--spacing-block)]`.

5. Info-Grid mit ScrollReveal und Delay wrappen:
   ```tsx
   <ScrollReveal className="grid grid-cols-1 sm:grid-cols-3 gap-8 mb-[var(--spacing-block)]" delay={0.15}>
     <div>Booking...</div>
     <div>Management...</div>
     <div>Label...</div>
   </ScrollReveal>
   ```
   Das bestehende `mb-16` wird zu `mb-[var(--spacing-block)]`.

6. Section Label bleibt OHNE ScrollReveal (sofort sichtbar).

Beide Pages bleiben Server Components (kein "use client").
  </action>
  <verify>
    <automated>cd /Users/constantinkaiser/claude-projects/now-website && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - shows/page.tsx importiert ScrollReveal
    - shows/page.tsx hat mindestens 2 ScrollReveal Wrapper
    - shows/page.tsx hat KEIN "use client" Directive
    - press/page.tsx importiert ScrollReveal
    - press/page.tsx hat mindestens 3 ScrollReveal Wrapper
    - press/page.tsx hat KEIN "use client" Directive
    - Beide Pages nutzen var(--spacing-section) oder var(--spacing-section-lg) fuer Section-Padding
    - Beide Pages nutzen var(--spacing-block) fuer Block-Abstande
    - npm run build laeuft fehlerfrei
  </acceptance_criteria>
  <done>Shows-Page und Press-Page haben ScrollReveal auf allen Content-Bloecken und konsistentes Spacing via Design Tokens. ANIM-01 ist damit fuer alle 4 Content-Pages erfuellt.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visuelle Verifikation aller Seiten -- Scroll-Reveals, Typografie, Spacing, Full-Bleed</name>
  <files>none</files>
  <action>
Scroll-Reveals auf allen 4 Content-Pages (About, Music, Shows, Press), uebergrosse fluide Typografie, konsistentes Section-Spacing, Full-Bleed Band-Foto auf About-Page, und CLS-freies Font-Loading mit Inter + Cormorant Garamond visuell pruefen.

Pruefschritte:
1. `npm run dev` starten (falls nicht schon laufend)
2. About-Page (http://localhost:3000/de/about): Langsam scrollen, Content faded ein, Band-Foto geht Edge-to-Edge, Spacing grosszuegig
3. Music-Page (http://localhost:3000/de/music): Featured Release, Spotify, Discography, Videos faden nacheinander ein, Headline gross und mutig
4. Shows-Page (http://localhost:3000/de/shows): Widget und Booking CTA faden ein
5. Press-Page (http://localhost:3000/de/press): Pressefotos, Text und Info-Grid faden ein
6. Browser-Resize (320px bis 1920px): Headlines skalieren smooth
7. Zoom-Test (Cmd+Plus 3x): Text wird groesser
8. Mobile-Viewport: Reveals funktionieren, kein horizontaler Scroll
  </action>
  <verify>User-Feedback: "approved" oder Issues beschreiben</verify>
  <acceptance_criteria>
    - User bestaetigt: Scroll-Reveals funktionieren auf allen 4 Pages
    - User bestaetigt: Full-Bleed Band-Foto auf About-Page korrekt
    - User bestaetigt: Typografie skaliert fluide
    - User bestaetigt: Kein horizontaler Scroll auf Mobile
  </acceptance_criteria>
  <done>User hat "approved" geantwortet oder Issues wurden gefixt und erneut verifiziert.</done>
</task>

</tasks>

<verification>
- `npm run build && npm run lint` laeuft fehlerfrei
- Alle 4 Content-Pages (About, Music, Shows, Press) haben ScrollReveal Wrappers
- Full-Bleed Sektion auf About-Page vorhanden
- Typography Tokens werden auf Headlines angewendet
- Spacing Tokens werden konsistent verwendet
- Kein horizontaler Scrollbalken
- Visuelle Verifikation durch User (Checkpoint)
</verification>

<success_criteria>
- ANIM-01 ist komplett: Alle Content-Sektionen auf allen 4 Pages haben Scroll-Reveals
- STRUC-01 auf Shows+Press: Konsistentes Spacing via Design Tokens
- User hat visuell verifiziert dass Reveals, Typografie, Spacing und Full-Bleed korrekt aussehen
- Build und Lint sind gruen
</success_criteria>

<output>
After completion, create `.planning/phases/02-scroll-reveals-typografie/02-03-SUMMARY.md`
</output>
