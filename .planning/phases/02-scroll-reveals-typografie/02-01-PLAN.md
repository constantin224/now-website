---
phase: 02-scroll-reveals-typografie
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - app/layout.tsx
  - app/[locale]/layout.tsx
autonomous: true
requirements: [TYPO-01, TYPO-02, TYPO-03, TYPO-04]

must_haves:
  truths:
    - "Headlines verwenden fluide clamp()-Groessen die von 320px bis 1920px smooth skalieren"
    - "Section-Spacing ist ueber Design Tokens einheitlich definiert"
    - "Letter-Spacing und Line-Height sind pro Hierarchie-Stufe festgelegt"
    - "Font-Loading verursacht keinen Layout Shift (CLS nahe 0)"
  artifacts:
    - path: "app/globals.css"
      provides: "Typography Scale Tokens, Spacing Tokens, Font-Family Tokens, full-bleed Utility"
      contains: "--text-display"
    - path: "app/layout.tsx"
      provides: "Cormorant Garamond mit Weight 700, Inter Body-Font via next/font"
      contains: "fontVariables"
    - path: "app/[locale]/layout.tsx"
      provides: "Beide Font-Variablen auf html-Element, overflow-x: clip auf body"
      contains: "fontVariables"
  key_links:
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "CSS Custom Properties --font-cormorant und --font-inter"
      pattern: "--font-cormorant|--font-inter"
    - from: "app/[locale]/layout.tsx"
      to: "app/layout.tsx"
      via: "fontVariables Import"
      pattern: "fontVariables"
---

<objective>
Typography Design System und Font-Optimierung aufsetzen -- fluide clamp()-Groessen, Section-Spacing Tokens, Letter-Spacing/Line-Height System, und Body-Font via next/font fuer CLS-freies Loading.

Purpose: Die typografische Grundlage fuer alle weiteren Seiten-Aenderungen schaffen. Ohne diese Tokens wuerden die Scroll-Reveals und Spacing-Aenderungen in Plan 02/03 mit Magic Numbers arbeiten.
Output: globals.css mit Design Tokens, layout.tsx mit zwei Fonts (Heading + Body), locale layout mit beiden Font-Variablen.
</objective>

<execution_context>
@/Users/constantinkaiser/.claude/get-shit-done/workflows/execute-plan.md
@/Users/constantinkaiser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-scroll-reveals-typografie/02-RESEARCH.md
@.planning/phases/01-animation-foundation/01-01-SUMMARY.md
@.planning/phases/01-animation-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Bestehende Exports die dieser Plan verwendet/aendert -->

From app/layout.tsx:
```typescript
export const fontVariable = cormorant.variable;
// WIRD GEAENDERT zu: export const fontVariables = `${cormorant.variable} ${inter.variable}`;
```

From app/globals.css @theme:
```css
@theme {
  --color-bg-base: #0e0e0e;
  --color-bg-section: #161210;
  --color-bg-card: #1e1814;
  --color-terracotta: #a07352;
  --color-sand: #d4cbbe;
  --color-sand-50: rgba(212, 203, 190, 0.5);
  --color-sand-38: rgba(212, 203, 190, 0.38);
  --color-line: rgba(160, 115, 82, 0.12);
  --font-body: system-ui, -apple-system, sans-serif;
}
/* WIRD ERWEITERT um Typography Scale, Spacing Tokens, Font-Family Tokens */
```

From app/[locale]/layout.tsx:
```typescript
import { fontVariable } from "@/app/layout";
// html className={fontVariable}
// WIRD GEAENDERT zu: import { fontVariables } from "@/app/layout";
// html className={fontVariables}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Typography Design Tokens in globals.css definieren</name>
  <files>app/globals.css</files>
  <read_first>
    - app/globals.css
    - .planning/phases/02-scroll-reveals-typografie/02-RESEARCH.md (Pattern 2: Typography Design Tokens)
  </read_first>
  <action>
Den bestehenden @theme Block in globals.css um folgende Design Tokens erweitern (bestehende Farb-Tokens NICHT aendern):

**Font-Family Tokens** (bestehenden --font-body ersetzen):
```css
--font-heading: var(--font-cormorant), "Times New Roman", serif;
--font-body: var(--font-inter), system-ui, -apple-system, sans-serif;
```

**Typography Scale** (alle mit clamp(), rem-basiert fuer WCAG-Zoom):
```css
--text-display: clamp(3rem, 2.4rem + 3vw, 6rem);
--text-h1: clamp(2.25rem, 1.8rem + 2.25vw, 4.5rem);
--text-h2: clamp(1.75rem, 1.4rem + 1.75vw, 3rem);
--text-h3: clamp(1.25rem, 1.1rem + 0.75vw, 2rem);
--text-body: clamp(1rem, 0.95rem + 0.25vw, 1.125rem);
--text-small: clamp(0.75rem, 0.7rem + 0.25vw, 0.875rem);
```

**Letter-Spacing pro Hierarchie-Stufe:**
```css
--tracking-display: -0.02em;
--tracking-heading: -0.01em;
--tracking-body: 0.01em;
--tracking-label: 0.15em;
```

**Line-Height System:**
```css
--leading-display: 1.1;
--leading-heading: 1.2;
--leading-body: 1.7;
--leading-tight: 1.3;
```

**Section Spacing Tokens:**
```css
--spacing-section: clamp(4rem, 3rem + 5vw, 8rem);
--spacing-section-lg: clamp(6rem, 4rem + 8vw, 12rem);
--spacing-block: clamp(3rem, 2rem + 4vw, 6rem);
```

Im @layer base Block ergaenzen:
1. Heading-Styles mit font-family, letter-spacing und line-height:
```css
h1, h2, h3, h4 {
  font-family: var(--font-heading);
  letter-spacing: var(--tracking-heading);
  line-height: var(--leading-heading);
}
```
2. Body overflow-x schuetzen:
```css
body { overflow-x: clip; }
```
3. Full-Bleed Utility-Klasse:
```css
.full-bleed {
  width: 100vw;
  margin-left: calc(50% - 50vw);
}
```
  </action>
  <verify>
    <automated>cd /Users/constantinkaiser/claude-projects/now-website && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - globals.css enthaelt --text-display mit clamp(3rem
    - globals.css enthaelt --text-h1, --text-h2, --text-h3 mit clamp()
    - globals.css enthaelt --spacing-section mit clamp(4rem
    - globals.css enthaelt --tracking-display, --tracking-heading, --tracking-body, --tracking-label
    - globals.css enthaelt --leading-display, --leading-heading, --leading-body
    - globals.css enthaelt --font-heading mit var(--font-cormorant)
    - globals.css enthaelt .full-bleed Klasse
    - globals.css enthaelt overflow-x: clip auf body
    - Bestehende Farb-Tokens (--color-bg-base etc.) sind unveraendert
    - npm run build laeuft fehlerfrei
  </acceptance_criteria>
  <done>Alle Typography-, Spacing- und Layout-Tokens sind in globals.css definiert. Build ist gruen.</done>
</task>

<task type="auto">
  <name>Task 2: next/font Setup mit Inter Body-Font und Cormorant Weight 700</name>
  <files>app/layout.tsx, app/[locale]/layout.tsx</files>
  <read_first>
    - app/layout.tsx
    - app/[locale]/layout.tsx
    - .planning/phases/02-scroll-reveals-typografie/02-RESEARCH.md (Beispiel 3: next/font Setup)
  </read_first>
  <action>
**app/layout.tsx aendern:**

1. Inter importieren: `import { Cormorant_Garamond, Inter } from "next/font/google";`

2. Cormorant Weight 700 hinzufuegen und Variable auf `--font-cormorant` aendern:
```typescript
const cormorant = Cormorant_Garamond({
  subsets: ["latin"],
  weight: ["300", "400", "600", "700"],
  variable: "--font-cormorant",
  display: "swap",
});
```

3. Inter Font hinzufuegen:
```typescript
const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});
```

4. Export aendern von `fontVariable` zu `fontVariables`:
```typescript
export const fontVariables = `${cormorant.variable} ${inter.variable}`;
```

WICHTIG: Die bestehende Variable hiess `--font-heading`, die neue heisst `--font-cormorant`. Das ist beabsichtigt weil globals.css jetzt `--font-heading: var(--font-cormorant)` definiert und so die Indirektion sauberer ist.

**app/[locale]/layout.tsx aendern:**

1. Import aendern: `import { fontVariables } from "@/app/layout";` (war: `fontVariable`)
2. html className aendern: `className={fontVariables}` (war: `className={fontVariable}`)

Sonst NICHTS an locale/layout.tsx aendern -- Navigation, Footer, SmoothScrollProvider, NewReleasePopup bleiben exakt gleich.
  </action>
  <verify>
    <automated>cd /Users/constantinkaiser/claude-projects/now-website && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - app/layout.tsx importiert Inter aus "next/font/google"
    - app/layout.tsx hat Cormorant mit weight ["300", "400", "600", "700"]
    - app/layout.tsx hat variable: "--font-cormorant" (nicht mehr "--font-heading")
    - app/layout.tsx exportiert fontVariables (nicht fontVariable)
    - app/[locale]/layout.tsx importiert fontVariables (nicht fontVariable)
    - app/[locale]/layout.tsx hat className={fontVariables} auf html
    - npm run build laeuft fehlerfrei (kein Import-Fehler durch Umbenennung)
  </acceptance_criteria>
  <done>Beide Fonts (Cormorant Garamond + Inter) sind via next/font geladen, adjustFontFallback ist aktiv, beide CSS-Variablen werden aufs html-Element gesetzt. Build ist gruen.</done>
</task>

</tasks>

<verification>
- `npm run build` laeuft fehlerfrei
- `npm run lint` meldet keine Fehler
- In Browser DevTools: CSS Custom Properties --text-display, --spacing-section, --font-heading, --font-body sind definiert
- Browser-Zoom (Cmd+Plus) skaliert Text korrekt (weil rem-basierte clamp-Werte)
</verification>

<success_criteria>
- Typography Design Tokens (Scale, Spacing, Tracking, Leading) sind in globals.css definiert
- Inter Body-Font und Cormorant Weight 700 sind via next/font geladen
- Beide Font-Variablen (--font-cormorant, --font-inter) werden aufs html-Element gesetzt
- Full-Bleed CSS Utility existiert
- overflow-x: clip auf body verhindert horizontalen Scroll
- Build und Lint sind gruen
</success_criteria>

<output>
After completion, create `.planning/phases/02-scroll-reveals-typografie/02-01-SUMMARY.md`
</output>
